<h4>New <%= @document.label.titleize %></h4>
<%= error_messages_for :document %>
<% form_for @document.becomes(Document), :url => admin_documents_path, :html => { :multipart => true } do |f| %>
  <%= render :partial => form_partial, :locals => { :f => f } %>
	<table cellpadding="0" cellspacing="0" border="0">
  <tr><td width="70"><%= f.submit 'Save Page' %></td><td width="55"> <%= link_to 'Cancel <b>X</b>', admin_cancel_document_path(@document), :class => 'button cancel' %></td></tr>
	</table>
<% end %>

  <% if session[:system] %>
  <%= form_partial %>
  <% end %>